# TODO

- [ ] Redesign the mascot as pixel art, matching the style of Claude's own
      pixel-art mascot (both states: working overtime + asleep, and the
      menu bar icons). See issue #1.
- [ ] Submit to awesome-claude-code + awesome-mac after 2026-08-24
      (list requires repos to be 14+ days old or 100+ stars).
- [ ] Apple Developer ID + notarization to remove the right-click-to-open
      friction on prebuilt releases.
- [ ] Intel build in releases (currently arm64 only; Intel users build
      from source).
